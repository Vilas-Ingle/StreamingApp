apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: streamingapp

spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
      - name: frontend
        image: 429965676677.dkr.ecr.ap-south-1.amazonaws.com/streamingapp-frontend:latest
        imagePullPolicy: Always

        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: streamingapp

spec:
  selector:
    app: frontend

  ports:
  - port: 80
    targetPort: 80

  type: LoadBalancer
